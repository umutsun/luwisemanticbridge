# Windows Docker Desktop Optimized Configuration - Alternative Ports
# Usage: docker compose --env-file ./.env.asemb -f docker-compose.windows-optimized.yml up --build

services:
  # PostgreSQL with pgvector extension
  postgres:
    image: pgvector/pgvector:pg16
    container_name: ${COMPOSE_PROJECT_NAME}-postgres-new
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "127.0.0.1:5433:5432"  # Alternative port
    volumes:
      - postgres_data_new:/var/lib/postgresql/data
      - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - asemb-network-new
    restart: unless-stopped

  # Redis for caching and queuing
  redis:
    image: redis:7-alpine
    container_name: ${COMPOSE_PROJECT_NAME}-redis-new
    command: >
      sh -c "
      if [ -n '${REDIS_PASSWORD}' ]; then
        redis-server --requirepass '${REDIS_PASSWORD}'
      else
        redis-server
      fi
      "
    ports:
      - "127.0.0.1:6380:6379"  # Alternative port
    volumes:
      - redis_data_new:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - asemb-network-new
    restart: unless-stopped

  # API Server
  api:
    build:
      context: ./api
      dockerfile: Dockerfile.windows
    container_name: ${COMPOSE_PROJECT_NAME}-api-new
    environment:
      - NODE_ENV=${NODE_ENV}
      - PORT=${API_PORT}
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - REDIS_HOST=postgres
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - JWT_SECRET=${JWT_SECRET}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - GOOGLE_AI_API_KEY=${GOOGLE_AI_API_KEY}
      - GROQ_API_KEY=${GROQ_API_KEY}
      - HUGGINGFACE_API_KEY=${HUGGINGFACE_API_KEY}
      - N8N_WEBHOOK_URL=${N8N_WEBHOOK_URL}
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    ports:
      - "127.0.0.1:8084:8083"  # Alternative port
    volumes:
      - ./api:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - asemb-network-new
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # Frontend (Next.js)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.windows
    container_name: ${COMPOSE_PROJECT_NAME}-frontend-new
    environment:
      - NODE_ENV=${NODE_ENV}
      - NEXT_PUBLIC_API_URL=http://localhost:8084
      - NEXT_PUBLIC_APP_URL=http://localhost:3006
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
    ports:
      - "127.0.0.1:3006:3000"  # Alternative port
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      - api
    networks:
      - asemb-network-new
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # RAG Anything Service
  rag-anything:
    build:
      context: ./rag-anything
      dockerfile: Dockerfile
    container_name: ${COMPOSE_PROJECT_NAME}-rag-anything-new
    environment:
      - UVICORN_HOST=0.0.0.0
      - UVICORN_PORT=8002
    ports:
      - "127.0.0.1:8003:8002"  # Alternative port
    volumes:
      - ./rag-anything:/app
    networks:
      - asemb-network-new
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  postgres_data_new:
    driver: local
  redis_data_new:
    driver: local

networks:
  asemb-network-new:
    driver: bridge
    ipam:
      config:
        - subnet: 172.21.0.0/16
          gateway: 172.21.0.1
    driver_opts:
      com.docker.network.driver.mtu: 1450
      com.docker.network.windowsshim.dnsservers: 8.8.8.8