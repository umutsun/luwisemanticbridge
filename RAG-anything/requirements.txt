# Core LightRAG packages - minimal install to save space
# Note: In production, you might want to install specific versions
# lightrag-hku is commented out to avoid heavy PyTorch dependencies
# Uncomment the line below if you need LightRAG functionality
# lightrag-hku

# Basic requirements for RAG service
huggingface_hub
python-dotenv
fastapi
uvicorn[standard]
pydantic
openai

# Progress bars for batch processing
tqdm

# MinerU 2.0 packages (replaces magic-pdf) - commented to save space
# mineru[core]

# Note: Uncomment the following lines if you need full RAG functionality:
# lightrag-hku
# mineru[core]
